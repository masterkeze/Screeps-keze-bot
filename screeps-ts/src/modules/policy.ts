export namespace Policy {
    /**
     * for each room, this method returns a corresponding list of group requirement.
     * @param  {Room} room
     * @returns name of groups
     */
    export function getPolicy(room:Room):string[]{
        let groupNames = [];
        return groupNames;
    }
}