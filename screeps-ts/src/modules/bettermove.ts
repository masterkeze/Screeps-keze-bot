/**
 * This module provides methods to 
 * 1. automatically swap creeps at the end of each tick.
 * 2. cache path and reuse path when calling moveTo
 * 3. automatically avoid deadly aera
 * 4. register obstacle position ( occupied position )
 */