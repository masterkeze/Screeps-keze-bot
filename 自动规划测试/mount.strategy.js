'use strict';
module.exports = function(){
    _.assign(global, Strategy);
}
// Singleton Object
// require所有的strategy，并提供访问它们的接口
const Strategy = {
    get : function(){

    },
    set : function(){

    }
}