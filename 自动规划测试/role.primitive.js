module.exports = {
	/** @param {Creep} creep */
	run: function(creep) {
        // register
        let data = creep.register();
        if (!data){
            creep.handle
            global.logWarn("primitive",`${creep.name} 警告:${没有组标识}`);

        }
        // do the job
        
	}
};
